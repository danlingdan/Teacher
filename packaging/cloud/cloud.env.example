# Copy to /etc/sqlteacher/cloud.env. Do not commit the real file.
SQLTEACHER_CLOUD_PORT=18080
SQLTEACHER_CLOUD_DB=/opt/sqlteacher/data/cloud.db
SQLTEACHER_UPDATE_MANIFEST=/opt/sqlteacher/shared/update-manifest.json
# Generate a random value of at least 32 characters. Rotating it changes anonymous rate-limit identities.
SQLTEACHER_FEEDBACK_HASH_SECRET=replace-with-a-random-32-character-minimum-secret
# Set only during first deployment, then remove or rotate these values.
SQLTEACHER_CLOUD_BOOTSTRAP_ADMIN_EMAIL=admin@example.edu
SQLTEACHER_CLOUD_BOOTSTRAP_ADMIN_PASSWORD=replace-with-a-random-12-character-minimum-password

# Optional outbound mail relay for password reset / email verification.
# The server writes mails to <data>/mails (FileMailSender outbox) regardless;
# set these to have an external relay deliver them. Never share credentials here.
SQLTEACHER_SMTP_HOST=
SQLTEACHER_SMTP_PORT=587
SQLTEACHER_SMTP_USER=
SQLTEACHER_SMTP_PASSWORD=
SQLTEACHER_SMTP_FROM=no-reply@sqlteacher.tech
