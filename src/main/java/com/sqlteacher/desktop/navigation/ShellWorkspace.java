package com.sqlteacher.desktop.navigation;

/** Stable top-level information architecture for the SQLTeacher 2.0 desktop shell. */
public enum ShellWorkspace {
    LEARNING,
    COURSE,
    EXPERIMENT,
    TEACHING,
    SYSTEM
}
