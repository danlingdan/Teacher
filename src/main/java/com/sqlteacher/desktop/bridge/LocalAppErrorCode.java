package com.sqlteacher.desktop.bridge;

public enum LocalAppErrorCode {
    INVALID_JSON,
    PAYLOAD_TOO_LARGE,
    INVALID_REQUEST,
    CONTRACT_VERSION_UNSUPPORTED,
    DUPLICATE_REQUEST,
    BUSY,
    CANCELLED,
    INVALID_METHOD_OR_PARAMS,
    LOCAL_APP_FAILURE
}
