package com.sqlteacher.application.collaboration;

/** Fine-grained permissions inside the settings center. */
public enum DesktopSettingPermission {
    APPEARANCE,
    LOCAL_CONNECTIONS,
    LOCAL_DATA_MAINTENANCE,
    TEACHING_DEFAULTS,
    CLOUD_OPERATIONS
}
