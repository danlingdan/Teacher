# SQLTeacher v1.2.0 阶段门禁与发布验收

日期：2026-07-22

平台：Windows 11、JDK 21、Maven；阿里云 ECS Ubuntu 24.04、OpenJDK 21、Nginx

## 自动化测试

- `mvn test`：246 项通过，0 失败，0 错误，0 跳过。
- 覆盖刷新令牌轮换与重放拒绝、Windows DPAPI 密文落盘、同步重试、任务状态机、跨角色授权、班级统计与 CSV 导出。
- 既有 SQL 风险分类、多语句拦截、AI 输出解析、本地数据库、备份恢复、FXML 和打包契约无回归。

## Windows 发布物

- `SQLTeacher-1.2.0.exe`：`3a58c321c6dbc413c15e708397b9334048210ee83803076a745ead8e7c7c4339`
- `SQLTeacher-1.2.0-windows-x64.zip`：`bfdde84a7d089020892780473e6dcbc68732b7b1edd58801433d429ba8eef33d`
- `package-stage1.ps1` 完整执行成功；app-image、EXE、ZIP 与 `SHA256SUMS.txt` 均已生成。
- app-image 的 `SQLTeacher.exe` 启动 5 秒未提前退出。

## 正式云端验收

正式入口：`https://api.sqlteacher.tech`。

| 场景 | 结果 |
|---|---|
| 发布前 SQLite 在线备份与完整性检查 | 通过，备份位于服务器受限备份目录 |
| 隔离恢复探针与恢复后完整性检查 | 通过 |
| `/opt/sqlteacher/current` 原子切换 | 已指向 `/opt/sqlteacher/releases/1.2.0`，旧 `1.1.0-dev` 保留用于回滚 |
| systemd 服务与 HTTPS 健康检查 | `active`，HTTP 200 |
| HTTP 跳转与 HSTS | HTTP 301，HSTS 存在 |
| 管理员登录、班级列表 | 通过 |
| 刷新令牌轮换、旧令牌重放 | 轮换通过，旧令牌 HTTP 401 |
| 班级统计与 CSV 导出 | 均 HTTP 200 |
| 登出撤销访问/刷新令牌 | 分别 HTTP 403 / 401 |

证书当前有效至 2026-10-20，Certbot 定时器继续定期重试。`certbot renew --dry-run` 未通过：Let’s Encrypt 多点 HTTP-01 校验链路返回 403；服务器端 challenge 文件为 87 字节且本机读取完整，改用 Certbot standalone 后验证节点仍在响应头后重置连接，排除应用与 Nginx 文件配置。服务器没有阿里云 DNS API 凭据，暂不能安全切换 DNS-01。必须在证书到期前配置最小权限 DNS 凭据并完成 DNS-01 演练，或由阿里云排查公网验证链路。

## 门禁结论

v1.2.0 功能、自动化、Windows 发布物、生产备份/恢复探针和正式 HTTPS 端到端门禁均通过，批准发布。证书续期 dry-run 作为有截止日期的运维问题保留，不影响当前证书和服务，但不得晚于 2026-09-20 完成修复与再次演练。
