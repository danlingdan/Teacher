# SQLTeacher v1.5.6 阶段验收记录

> 验收日期：2026-07-30
> 候选版本：`v1.5.6`

## 交付范围

- 浅色模式和禁用控件文字对比度修复。
- 本地 AI 与网络 AI 统一入口及可审查 SQL 草稿扩展。
- 学生练习字段摘要、编辑空间和三级提示。
- 教师任务截止时间与反馈目标选择控件。
- 默认关闭、明确警告并在本机持久化的 SQL 无限模式。

## 自动门禁

| 检查项 | 结果 | 说明 |
|---|---|---|
| `mvn test` | 通过 | 282 项测试，0 失败、0 错误、0 跳过；构建版本为 1.5.6。 |
| Windows EXE/ZIP/app-image | 通过 | 已生成 `SQLTeacher-1.5.6.exe`、`SQLTeacher-1.5.6-windows-x64.zip` 和 app-image，统一位于 `target/installer`。 |
| SHA-256 | 通过 | 清单仅包含 1.5.6 EXE 与 ZIP；本机重新计算后两项均匹配。 |
| 生产云端地址 | 通过 | 默认地址为 `https://api.sqlteacher.tech`；公开 HTTP 地址仍被拒绝。 |
| 敏感信息与运行数据 | 通过 | 拟提交路径不含 `.secrets`、`app-data`、`target`、数据库或日志；差异未发现疑似密钥值。ZIP 同样未包含这些运行数据。 |
| Git 与 GitHub Release | 待执行 | 提交、推送、标签、Actions 和公开附件将在发布后回填。 |

## 验收边界

- 按发布负责人的要求，不启动 JavaFX 做 UI 自动验证，由负责人安装后自行验收。
- 不下载 GitHub Actions 重新构建的附件，也不比较跨构建 Java 包哈希。
- 无限模式不改变 AI 只能生成草稿的约束，也不能绕过数据库自身权限和驱动限制。
