# SQLTeacher v1.6.0 阶段验收记录

> 验收日期：2026-07-30
> 候选版本：`v1.6.0`

## 交付范围

- 多 Provider Profile、DPAPI 凭据、统一能力探测与模型发现。
- NL2SQL、SQL 错误解释和教师反馈草稿统一任务编排。
- 上下文最小化、隐私预览、SQL 草稿修订和重新安全审查。
- 设备用量边界、教师反馈模板、脱敏历史与主动收藏。
- Ollama 模型切换自动卸载上一模型。

## 自动门禁

| 检查项 | 结果 | 说明 |
|---|---|---|
| 聚焦测试 | 通过 | Provider、DPAPI、上下文、任务编排、模型切换、NL2SQL 与 Spring 接线通过。 |
| 真实 Provider | 通过 | 本地 `qwen3.5:0.8b` 与 DeepSeek 网络调用均通过统一任务层；真实测试默认禁用。 |
| `mvn test` | 通过 | 295 项，0 失败、0 错误、2 跳过；跳过项为已单独验证通过的真实 Provider 冒烟。 |
| 密钥与运行数据 | 通过 | 本地测试密钥位于 Git 忽略的 `.secrets`；Git 跟踪文件精确密钥命中数为 0。 |
| Windows EXE/ZIP/app-image | 通过 | 已生成 `SQLTeacher-1.6.0.exe`、`SQLTeacher-1.6.0-windows-x64.zip` 和 app-image，统一位于 `target/installer`；旧版本已移出该目录。 |
| SHA-256 | 通过 | EXE 为 `49dc73d379531daeb6540c38550d8c8a38d8a6d86d19045dddfc3da625f79fef`；ZIP 为 `6c087f491408726f2eb8ba67792fd35845102df5130aa0d87a5f717b39c8bdc7`，均与清单一致。 |
| 产物内容 | 通过 | ZIP 共 323 项，不含 `.secrets`、`app-data`、数据库或日志；EXE、ZIP、app-image 中测试密钥精确命中数为 0；生产云地址配置正确。 |
| Git 与 GitHub Release | 待执行 | 提交、推送、标签和公开 Release 完成后回填。 |

## 验收边界

- 不启动 JavaFX 做人工 UI 验收，由发布负责人安装后自行检查。
- 按发布负责人既有要求，不下载 GitHub Actions 重新构建的附件，不比较跨构建 Java 包哈希。
- `.secrets`、`app-data` 与 `target` 均不得进入提交或发布附件。
