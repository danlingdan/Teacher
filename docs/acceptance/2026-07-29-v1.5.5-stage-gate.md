# SQLTeacher v1.5.5 阶段验收记录

> 验收日期：2026-07-29
> 发布基线：`v1.5.0`
> 候选版本：`v1.5.5`

## 1. 功能验收

| 验收项 | 状态 | 证据 |
|---|---|---|
| 现代控件基线 | 通过 | AtlantaFX Primer 深浅主题由 `UiPreferencesService` 随应用主题切换。 |
| 语义设计令牌 | 通过 | foundation、components、layouts 三层资源存在并由 Scene 统一加载。 |
| 自适应应用壳 | 通过 | 840/1040/1360px 断点分类自动化测试通过；侧栏宽度、文本和可访问信息按模式切换。 |
| 首页布局 | 通过 | 首页使用自动换列 `TilePane`，保留按钮语义与键盘操作。 |
| SQL 工作区 | 通过 | 示例、编辑器、执行按钮和结果区在同页可见；编辑器/结果使用垂直 `SplitPane`。 |
| 键盘焦点 | 通过 | 不再清除导航焦点；核心控件使用语义焦点色和明确边框。 |
| 安全边界 | 通过 | 未修改 SQL 执行、风险分析、连接凭据、AI 草案或权限服务。 |

## 2. 聚焦验证

```powershell
mvn -q "-Dtest=UiLayoutModeTest,UiPreferencesServiceTest,DesktopVisualResourceTest,DesktopFxmlResourceTest" test
```

结果：通过，0 失败、0 错误。

```powershell
mvn -q javafx:run
```

结果：深色登录页、访客首页和 SQL 工作区均成功显示；无 FXML 加载异常或 CSS 解析警告。窗口约 1180×750 时首页三列卡片完整，SQL 编辑器、执行动作和结果区同时可见。随后使用 `SQLTEACHER_SYSTEM_THEME=light` 独立启动，浅色登录页、输入状态和控件层级显示正常。

## 3. 最终门禁

| 门禁 | 状态 | 记录 |
|---|---|---|
| `mvn test` | 通过 | 274 项测试，0 失败、0 错误、0 跳过；构建版本显示为 1.5.5。 |
| Windows EXE/ZIP/app-image | 通过 | 生成 `SQLTeacher-1.5.5.exe`、`SQLTeacher-1.5.5-windows-x64.zip`、app-image 和 `SHA256SUMS.txt`；app-image 窗口成功显示。 |
| SHA-256 | 通过 | ZIP `4f52023e665a0e9ddaf4e0feea2afc05e1fd3893618115761bde40a3f4e5647b`；EXE `f8d0012dde7ec130ec6b93eb3791054694359f409880af30f0a39b223b2a35d8`；均与清单匹配。 |
| 包内容 | 通过 | ZIP 共 323 项，包含 `atlantafx-base-2.1.0.jar`；未发现 `.secrets`、`app-data`、数据库或日志文件。 |
| Git 变更检查 | 通过 | `git diff --check` 通过；变更仅包含 v1.5.5 UI Foundation、版本、测试和记录文件，生成产物保持忽略。 |
| `main` 与 `v1.5.5` 标签 | 通过 | `main` 已推送至提交 `062e295`；带注释标签 `v1.5.5` 已推送。 |
| GitHub Actions | 通过 | 发布工作流 [30467793510](https://github.com/danlingdan/Teacher/actions/runs/30467793510) 成功，构建提交为 `062e295547a0540e27b194267a68e873e2bb53d2`。 |
| GitHub Release | 通过 | [SQLTeacher v1.5.5](https://github.com/danlingdan/Teacher/releases/tag/v1.5.5) 已公开发布，非草稿、非预发布，包含 EXE、Windows x64 ZIP 和 `SHA256SUMS.txt` 三个附件。 |

v1.5.5 已完成发布。按发布负责人的明确指示，本次不下载 GitHub Release 附件，也不比较本地打包与 GitHub Actions 重新打包产物的哈希；发布结论以本地门禁、远端工作流成功状态和公开 Release 附件元数据为准。
