# v2.0.0 正式发布门禁

> 日期：2026-08-09
> 状态：正式发布进行中

## 冻结与版本

- GA 使用 RC.1 已冻结的产品契约，不新增功能、schema、活动类型、Cloud 主端点或顶层 UI 架构。
- Maven、JAR、Windows 包、Git 标签、Release 和签名更新清单必须统一为 `2.0.0` / `v2.0.0`。
- 稳定安装只接受 `stable` 更新清单，拒绝 Alpha、Beta 和 RC 通道。

## 本地与发布门禁

| 门禁 | 结果 |
| --- | --- |
| GA 聚焦测试与文档检查 | 待执行 |
| 发布提交后的 `mvn clean test` | 待执行 |
| 发布性能验证 | 待执行 |
| EXE / ZIP / app-image / SHA-256 / SBOM | 待执行 |
| ZIP 敏感内容扫描与 app-image 冒烟 | 待执行 |
| JAR `Build-Commit` 与标签提交 | 待执行 |
| GitHub Actions / 正式 Release / stable 更新清单 | 待执行 |
| 生产备份、部署、回环与公网 HTTPS | 待执行 |

## 发布边界

正式 Release 必须为非 draft、非 prerelease 并标记 Latest。生产更新必须先在线备份并验证完整性，以原 `2.0.0-alpha.7` release 和发布前备份为回滚点；Java API 继续只监听 `127.0.0.1:18080`。
