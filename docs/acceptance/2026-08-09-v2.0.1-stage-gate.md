# SQLTeacher v2.0.1 正式发布门禁

> 日期：2026-08-09
>
> 版本：2.0.1

## 范围

本版本只调整用户可见文案及其回归约束。数据库 schema 保持 17，Cloud API、活动类型、课程内容和安全执行路径不变，因此不需要更新生产 ECS。

## 本地门禁

| 检查 | 结果 |
| --- | --- |
| 聚焦测试 | `AppI18nTest`、`SimulationActivityEvaluatorTest` 通过 |
| 完整测试 | 466 项，0 失败，0 错误，2 项按环境条件跳过 |
| Windows 打包 | `SQLTeacher-2.0.1.exe`、便携 ZIP、app-image、校验和与 CycloneDX SBOM 均已生成 |
| 版本和 Cloud 地址 | Maven 版本为 `2.0.1`，打包配置仍使用 `https://api.sqlteacher.tech` |
| 文档与差异 | 链接检查和 `git diff --check` 必须在发布提交前通过 |

## 发布门禁

正式标签必须为 `v2.0.1`。GitHub Actions 应在标签源码上重新运行完整测试与打包，使用 `stable` 通道签名更新清单，并发布非草稿、非预发布且标记为 Latest 的 Release。最终资产摘要以 Release 附带的 `SHA256SUMS.txt` 为准。

发布说明见 [v2.0.1](../releases/v2.0.1.md)。
