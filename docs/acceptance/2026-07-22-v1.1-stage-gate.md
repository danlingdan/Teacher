# SQLTeacher v1.1.0 阶段门禁与测试报告

日期：2026-07-22
平台：Windows 11、JDK 21、Maven；阿里云 ECS Ubuntu 24.04、OpenJDK 21、Nginx

## 自动化与打包

- `mvn test`：237 项通过，0 失败，0 错误，0 跳过。
- Windows app-image 与 `SQLTeacher-1.1.0-windows-x64.zip` 生成成功。
- 打包后的 `SQLTeacher.exe` 启动 5 秒未提前退出。
- FXML 资源检查已覆盖新增 `cloud-center.fxml`。
- 云端教学页完成实际 JavaFX 渲染走查：卡片层级、滚动、未登录禁用态和空表单错误反馈均正常。
- 启动登录门禁、访客入口、身份标签、账号切换和按角色隐藏导航完成实际 JavaFX 走查。

## 云端端到端验收

公网测试入口：`http://8.130.47.235`。

| 场景 | 结果 |
|---|---|
| 健康检查与管理员登录 | 通过 |
| 学生注册、班级创建、添加学生/教师 | 通过 |
| 服务端班级成员关系授权 | 通过 |
| 教师发布班级任务、班级成员读取任务 | 通过 |
| 学生注册后由教师邀请、重新登录获得教师权限 | 通过 |
| 学生创建班级被拒绝 | 通过，HTTP 403 |
| 登出后旧令牌继续访问 | 被拒绝，HTTP 403 |
| 学习事件上传与增量下载 | 通过 |
| 同一事件重复上传 | 幂等，云端仅保留一条 |
| 访客与其他账号的本地事件进入当前账号同步 | 已隔离，不上传 |

## 门禁结论

v1.1 账号、班级、多教师、任务发布、角色授权、学习事件同步、云端教学页、网络 AI 和 UI 可用性增量均已完成并通过当前环境验收。HTTP 测试入口是明确的临时限制，不得用于正式学生数据；启用正式域名与 HTTPS 后方可转入教学使用。
