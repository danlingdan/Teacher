# 2026-09-18 v3.6.0 发布与更新清单部署记录

- **发布方式**：按用户指示，本次发布使用**本地构建产物**并跳过 CI 工作流（回执提交消息带
  `[skip ci]`，tag 推送未触发 release.yml；无 CI 构建产物）。流程沿用
  [v3.5.4 本地签名发布先例](2026-09-18-v3.5.4-release-deployment.md)。
- **版本**：`v3.6.0`（知识库质量版本）。提交 `a7ba197`（发布内容）+ `79d9903`（LEG-15 回执），
  tag `v3.6.0` 绑定回执提交。
- **本地门禁**：`mvn test` 804/0/2（1 处失败为 `SqliteAppDatabaseInitializerTest` 尚期望
  schema 版本 25，迁移 26 后已修正为 26 并复跑全量）、`npm test` 179、`npm audit --omit=dev`
  0 漏洞、`npm run build`、`cargo test --lib` 5、`ReleaseVerificationApp` 5 项 PASS；
  `package-v3.ps1` 产物 SQLTeacher-3.6.0.exe / -windows-x64.zip，SHA256SUMS 一致，
  installer 目录无陈旧版本文件，ZIP 232 项安全扫描无可疑路径，portable 冒烟启动正常。
- **Release**：`gh release create v3.6.0`（基于已推送的远端 tag，非 draft），`gh release upload
  --clobber` 上传 7 项本地产物（EXE、ZIP、SHA256SUMS.txt、两个 SBOM、本地签名
  update-manifest.json、verification-receipt.json），大小与本地一致；`releases/latest` 指向
  v3.6.0。首建草稿误用 `--target main` 产生 untagged 关联，已删除草稿并基于既有 tag 重建。
- **签名清单**：`UpdateManifestTool` 以 `release-2026-01` 密钥本地签名（payload 指向
  v3.6.0 Release 资产 URL，尺寸/SHA-256 取自本地构建实物）。
- **ECS 部署**：部署前备份 `/opt/sqlteacher/shared/update-manifest-v3.5.4.json`；SFTP 上传至
  `/tmp/.update-manifest-v360.tmp`，远端 sha256 与本地一致后
  `install -o root -g sqlteacher -m 0640` 原子替换（paramiko 密码认证，凭据不落日志）。
- **公网验证**：`GET /api/v1/app/update-manifest` → keyId `release-2026-01`、version `3.6.0`、
  installer/portable 尺寸与 SHA-256 与 Release 资产一致、rollout 100%。
- **回写（DOC-8/9）**：`docs/README.md` 当前基线 → 3.6.0；`docs/releases/README.md` 头部状态
  与索引；本发布说明日期行；`docs/plans/README.md` 与计划文档状态；GitHub Release 正文带完整
  更新日志与真实日期且为 Latest。
- **遗留提示**：KBQ-2（本地嵌入链路）在积压清单；`update-manifest-v3.5.4.json` 备份保留在
  ECS 原目录。

返回 [迭代计划索引](../plans/README.md) · [文档中心](../README.md)。
