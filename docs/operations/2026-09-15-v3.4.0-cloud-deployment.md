# SQLTeacher v3.4.0 云端部署与更新清单发布记录

> 部署时间：2026-09-15 01:36–01:44（Asia/Shanghai）
> 生产域名：`https://api.sqlteacher.tech`
> 应用发布：v3.4.0 标签（`57e16f7`，GitHub Actions run `34873975313` 成功并发布 Latest）
> 目标目录：`/opt/sqlteacher/releases/3.4.0`

## 部署内容

1. 服务端应用从 v3.3.4 升级到 v3.4.0（质量版本，见 `docs/releases/v3.4.0.md`）。
   服务端实质变更：题库迁移崩溃安全顺序修复（BUG-1，2026-09-12 生产事故跟进）、
   云端 schema 统一为 `CloudSchemaMigrator` 8 版本化迁移（含缺口自愈，REF-5）、
   加列改 `PRAGMA table_info` 判列（BUG-3）、统计/导出解析失败计数+日志（BUG-10）、
   `SqlTeacherCloudServer` 拆分与 store 家族去重（REF-1/2/3）、邮件 base URL 可注入
   （REF-6）、HTTP 客户端收敛（REF-7）。
2. 运行时沿用版本目录内 JDK 25（`runtime/jdk25` 从 3.3.4 版本目录复制，
   Temurin 25.0.4.1，`java -version` 验证）。
3. 稳定通道更新清单替换为 v3.4.0（rollout 100%、未暂停）。旧清单（3.3.4，
   SHA-256 `79a27ed472ae37c27fb9fb8c604bf7e86d37554bcbd1ffd7f8f903e597e1b91f`）
   备份为 `/opt/sqlteacher/shared/update-manifest.json.backup-20260914T174154Z`。

## 上线前门禁与状态

- v3.4.0 已通过完整发布门禁并公开 Release（非草稿、非预发布、`releases/latest`
  指向 v3.4.0，6 项资产齐全）：本地 `mvn test` 706 项通过（2 项固有跳过）、
  vitest 23 文件 79 项通过、`npm audit --omit=dev` 0 漏洞、`cargo test` 5 项通过、
  `ReleaseVerificationApp` 5 项 PASS、本地打包（JDK 25）EXE/ZIP/SHA256SUMS/双 SBOM
  校验通过、便携版冒烟启动成功且强杀后 sidecar 被 Job Object 连带回收（BUG-4 实证）。
  完整证据见 `docs/acceptance/2026-09-14-v3.4.0-quality-gate.md`。
- release workflow 首跑（run `34873816383`）在"WSL 探测"步骤失败：Actions pwsh
  包装器把 `wsl.exe` 探测的非零退出码当作步骤退出码，降级逻辑成功仍被判失败
  （该步骤仅在打 tag 时执行，此前从未真实运行）。修复（`57e16f7`，显式 `exit 0`）
  后将 v3.4.0 tag 重打到修复提交；原 tag 仅存在约 2 分钟且未产生任何 Release/draft。
  二跑成功（18m13s，WSL 不可用降级 `-Pfast` 警告属预期）。
- probe：服务 active，`current → releases/3.3.4`，回环 `/health` ok
  （knowledgeIndex ready、backlog 0、signedUpdates available），磁盘剩余约 9.5 GB。
- 在线备份（升级前）：`/opt/sqlteacher/backups/cloud-20260914T174021Z.db`，
  892,928 bytes，SHA-256
  `1aa6c463cd6075be15d38a3ae78dc78d90f9f1f45a819683bff79d958dcd7430`，
  `pragma integrity_check` ok。

## 部署过程

- 部署包 `cloud-release-3.4.0.tgz`（`Teacher-3.4.0.jar` + 32 个运行时依赖——较
  3.3.4 少 1 个，因 REF-21 将 h2 降为 test scope；139,407,787 bytes）上传，
  本地与远端 SHA-256 均为
  `42825324719e9d654660cf82c4b55f333807cc1ca57ba5e64042d2e71e39b1bf`。
- `deploy-v111.py swap` 原子切换 `current → releases/3.4.0` 并重启
  `sqlteacher-cloud.service`，结果 `SWAP_OK`（bin/ 运维脚本自动复制）。
- 更新清单：CI 产出的签名清单先在本地完成 Ed25519 验签（公钥
  `release-2026-01`，签名覆盖 payload 原始字节），payload 内 installer/portable
  SHA-256 与 CI `SHA256SUMS.txt` 一致；上传服务器 shared 目录采用
  `install -o root -g sqlteacher -m 640`，上传前后字节级一致
  （新清单 SHA-256
  `04e56bf6115b5f8e1e85871db258aff5d8ae40a11f76fdba415a8c7ee5e3fe8f`）。

## 上线后验证

- 服务 active，`/opt/sqlteacher/current → /opt/sqlteacher/releases/3.4.0`。
- 回环与公网 HTTPS `/health` 均 `status=ok`（TLS 校验通过），knowledgeIndex
  ready、backlog 0；capabilities 完整（含 BATCH_SUBMISSION_STATUS、ROLLOUT）；
  切换后 error/exception 级日志 0 条。
- `CloudSchemaMigrator` 接管生效：`cloud_schema_version` 8 个版本全部盖戳
  （1–6 为历史状态回填、7–8 于本次启动应用，缺口自愈按设计工作）。
- **BUG-1 生产实证**：事故停放表 `exercise_bank_blocks_v1_migrated_20260912`
  91 个 block 与现行表 91 个完全对齐，滞留 0（幂等回灌闭环，2026-09-12 事故
  跟进项自此关闭）。
- 公网 `/api/v1/app/update-manifest` 返回内容与本地 CI 产物字节级一致（SHA-256
  `04e56bf6…`），payload version=3.4.0、channel=stable、rollout 100%、未暂停。
- 周边服务：qdrant、embedding、backup timer、certbot timer、operations-check
  timer 均 active。

## 回滚方案

- 服务回滚：`ln -sfn /opt/sqlteacher/releases/3.3.4 /opt/sqlteacher/current.new
  && mv -T /opt/sqlteacher/current.new /opt/sqlteacher/current
  && systemctl restart sqlteacher-cloud`。
- 数据回滚：恢复升级前备份
  `/opt/sqlteacher/backups/cloud-20260914T174021Z.db`（先做当前库副本）。
- 清单回滚：恢复
  `/opt/sqlteacher/shared/update-manifest.json.backup-20260914T174154Z`。

## 已知限制

- 更新清单 `minimumSupportedVersion` 仍为 `1.9.0`（提示闸门），而 CMP-0 决策后
  桌面端仅保证 v3.x（有 schema_version 表）数据库升级、更老库启动时明确拒绝并
  保留原文件；两者不矛盾（清单字段只影响更新弹窗提示，数据保护由迁移器强制），
  是否上抬该字段留待 v3.5.0 评估。
- release workflow 的 WSL 探测在 GitHub 托管 runner 上恒降级 `-Pfast`
  （integration/runner/live 标签测试不在 CI 发布门禁内跑）；本机发布前已跑
  全量 `mvn test`（706 项）覆盖该缺口。

返回 [操作记录索引](README.md)。
