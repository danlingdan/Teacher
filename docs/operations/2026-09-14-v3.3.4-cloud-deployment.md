# SQLTeacher v3.3.4 云端部署与更新清单发布记录

> 部署时间：2026-09-14 02:34–02:56（Asia/Shanghai）
> 生产域名：`https://api.sqlteacher.tech`
> 应用发布：v3.3.4 标签（GitHub Actions run `34775054761` 成功并发布 Latest）
> 目标目录：`/opt/sqlteacher/releases/3.3.4`

## 部署内容

1. 服务端应用从 v3.3.3 升级到 v3.3.4。本次为纯客户端体验修复版本（2026-09-14
   全界面 UI 审计修复，见 `docs/releases/v3.3.4.md` 与
   `docs/plans/2026-09-14-ui-audit-findings-and-fix-plan.md`），无 IPC 契约变更、
   无 schema 变更、无新增服务端端点；服务端代码实际无行为差异，升级以保持
   版本号与桌面端一致。
2. 运行时沿用版本目录内 JDK 25（`runtime/jdk25` 从 3.3.3 版本目录复制，
   Temurin 25.0.4.1+1，`java -version` 验证）。
3. 稳定通道更新清单替换为 v3.3.4（rollout 100%、未暂停）。旧清单（3.3.3，
   SHA-256 `e221dec5995b7f046ea667a95423724b51ef0ee336906c4ea319ad36988d0170`）
   备份为 `/opt/sqlteacher/shared/update-manifest.json.backup-20260913T185500Z`。

## 上线前门禁与状态

- v3.3.4 已通过完整发布门禁并公开 Release（非草稿、非预发布、`releases/latest`
  指向 v3.3.4）：`mvn test` 604 项通过（2 项固有跳过）、vitest 18 文件 59 项通过、
  `cargo test`/`cargo check` 通过、本地打包（JDK 25）EXE/ZIP/SHA256SUMS/双 SBOM
  校验通过（`tr -d '\r'` 后 `sha256sum -c` OK；ZIP 内无 `.env`/`.secrets`/数据库/
  日志；打包内 `sidecar.json` contractVersion 3.0-v1、applicationVersion 3.3.4）。
- probe：服务 active，`current → releases/3.3.3`，回环 `/health` ok
  （knowledgeIndex ready、backlog 0），磁盘剩余约 9.9 GB。
- 在线备份（升级前）：`/opt/sqlteacher/backups/cloud-20260913T184010Z.db`，
  851,968 bytes，SHA-256
  `4a9d857f121814137735ea606f16218ed8c90ed01f4f4f75469b9c9d2925efc4`，
  `pragma integrity_check` ok。

## 部署过程

- 部署包 `cloud-release-3.3.4.tgz`（`Teacher-3.3.4.jar` + 33 个运行时依赖，
  141,920,402 bytes）上传，本地与远端 SHA-256 均为
  `0134555e1efee479981d1f71be277f0f9a2e7e80f469fa1a89189460e54472bc`。
- `deploy-v111.py swap` 原子切换 `current → releases/3.3.4` 并重启
  `sqlteacher-cloud.service`，结果 `SWAP_OK`（bin/ 运维脚本自动复制）。
- 更新清单：CI 产出的签名清单先在本地完成 Ed25519 验签（公钥
  `release-2026-01`，签名覆盖 payload 原始字节），payload 内 installer/portable
  SHA-256 与 CI `SHA256SUMS.txt` 一致；上传服务器 shared 目录采用
  `install -o root -g sqlteacher -m 640`（保留权限模式），上传前后字节级一致
  （新清单 SHA-256
  `79a27ed472ae37c27fb9fb8c604bf7e86d37554bcbd1ffd7f8f903e597e1b91f`）。

## 上线后验证

- 服务 active，`/opt/sqlteacher/current → /opt/sqlteacher/releases/3.3.4`，
  进程运行于 `current/runtime/jdk25/bin/java`，classpath 指向
  `Teacher-3.3.4.jar`。qdrant、embedding 均 active。
- 回环与公网 HTTPS `/health` 均 `status=ok`（TLS 校验通过），knowledgeIndex
  ready、backlog 0；切换后 10 分钟内 error 级日志 0 条。
- 公网 `/api/v1/app/update-manifest` 返回内容与本地 CI 产物字节级一致（SHA-256
  `79a27ed4…`），payload version=3.3.4、channel=stable、rollout 100%、未暂停。
- schema 检查：`account_tasks`、`reset_tokens`、`email_verifications` 均在。

## 回滚方案

- 服务回滚：`ln -sfn /opt/sqlteacher/releases/3.3.3 /opt/sqlteacher/current.new
  && mv -T /opt/sqlteacher/current.new /opt/sqlteacher/current
  && systemctl restart sqlteacher-cloud`。
- 数据回滚：恢复升级前备份
  `/opt/sqlteacher/backups/cloud-20260913T184010Z.db`（先做当前库副本）。
- 清单回滚：恢复
  `/opt/sqlteacher/shared/update-manifest.json.backup-20260913T185500Z`。

## 已知限制

- 命令面板键盘 Enter 导航在验证环境中一次成功、数次未触发（与浏览器验证环境
  崩溃恢复相关，点击路径始终稳定）；随包发布后建议人工按一次 Ctrl+K 复核。
- `migrateFromLegacySchema` 的"先改名后回灌"顺序缺陷（3.3.3 部署记录中的跟进项）
  本版未触及服务端 schema，不影响本次部署；仍待后续代码修复。

返回 [操作记录索引](README.md)。
