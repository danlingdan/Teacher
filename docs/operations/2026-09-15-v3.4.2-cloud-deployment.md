# SQLTeacher v3.4.2 云端部署与更新清单发布记录

> 部署时间：2026-09-15 05:35–05:45（Asia/Shanghai）
> 生产域名：`https://api.sqlteacher.tech`
> 应用发布：v3.4.2 标签（`57a4eab`，GitHub Actions run `34899280729` 成功，11m22s，
> 公开 Release Latest 于 2026-09-15 05:43:06 CST）
> 目标目录：`/opt/sqlteacher/releases/3.4.2`

## 部署内容

1. 服务端应用从 v3.4.0 升级到 v3.4.2（合并交付 v3.4.1+v3.4.2，见
   `docs/releases/v3.4.2.md`）。服务端实质变更：**云端 schema 升至 Migration 9**
   （`classrooms.join_code` 文本列 + 唯一索引 `idx_classrooms_join_code`，存量班级
   由 `CloudClassroomStore` 构造时回填 8 位短码）；班级码自助加入端点
   `POST /api/v1/classes/join` 与教师端 `GET …/join-code`、`POST …/join-code/rotate`
   （原 v3.4.1 CLS）。问题反馈通道（LEG-10）服务端为 v1.10 既有能力，无服务端改动。
   契约全部 additive，遵循"先部署云端、再发桌面版"，旧客户端不受影响。
2. 运行时沿用版本目录内 JDK 25（`runtime/jdk25` 从 3.4.0 版本目录 `cp -a` 复制并
   `chmod -R a+rX`，Temurin 25.0.4.1，从 `releases/3.4.2/runtime/jdk25/bin/java`
   验证 `java -version`）。
3. 稳定通道更新清单替换为 v3.4.2（rollout 100%、未暂停）。旧清单（3.4.0，
   SHA-256 `04e56bf6115b5f8e1e85871db258aff5d8ae40a11f76fdba415a8c7ee5e3fe8f`）
   备份为 `/opt/sqlteacher/shared/update-manifest.json.backup-20260914T214446Z`。

## 上线前门禁与状态

- v3.4.2 已通过完整发布门禁并公开 Release（非草稿、非预发布、`releases/latest`
  指向 v3.4.2，7 项资产齐全：EXE、Windows x64 ZIP、`SHA256SUMS.txt`、Java/UI 双
  CycloneDX SBOM、`update-manifest.json`、`verification-receipt.json`）：本地
  `mvn test` 723 项通过（0 失败 / 0 错误 / 2 项固有跳过）、vitest 98 项通过、
  `npm audit` 0 漏洞、`cargo test` 5 项通过、`ReleaseVerificationApp` 5 项 PASS、
  本地打包（JDK 25）EXE/ZIP/SHA256SUMS/双 SBOM 校验通过、NSIS 许可协议页与
  `legal\` 资源断言通过、便携版无控制台冒烟通过。
- **LEG-15 首次行使**：本地全量测试结果写入 `verification-receipt.json`（绑定
  `a3988a9`），CI 凭据评估接受后跳过 Java/WSL/前端三项测试步骤；`npm ci`、
  `npm audit`、打包、签名、契约校验照常运行。run 注解确认 "Local verification
  receipt accepted (a3988a9…); CI test execution skipped"。
- release workflow 首跑（run `34899057598`）在"评估本地验证凭据"步骤失败：
  `actions/checkout` 默认 `fetch-depth: 1` 未取父提交致 `commitOk=False`，且
  fail-safe 分支末条 `git rev-parse` 的非零退出码被 Actions pwsh 包装器当作步骤
  退出码（与 `57e16f7` 修复的 WSL 探测同类陷阱）。修复（`a3988a9`：`fetch-depth: 0`
  + 显式 `exit 0`）后将凭据重绑修复提交、v3.4.2 tag 重打到 `57a4eab`；原 tag 仅
  存在约 2 分钟且未产生任何 Release/draft。二跑成功（11m22s）。
- 凭据脚本另修复一处：`java -version` 把版本横幅写入 stderr，`ErrorActionPreference=Stop`
  下重定向的原生 stderr 触发 `NativeCommandError` 终止脚本（`b194f11`，对两处探测
  临时放宽偏好）。
- probe（只读）：服务 active，`current → releases/3.4.0`，回环 `/health` ok
  （knowledgeIndex ready、backlog 0、signedUpdates available），`cloud_schema_version`
  8 个版本，classrooms 4 个（无 `join_code` 列），`pragma integrity_check` ok，
  磁盘剩余约 9.2 GB，qdrant/embedding/各 timer active，最新备份
  `cloud-20260914T193403Z.db`。
- 在线备份（升级前）：`/opt/sqlteacher/backups/cloud-20260914T213535Z.db`，
  892,928 bytes，SHA-256
  `31c0ef8d5d97fbdc10ca6cfcccd002a21602292ab312aa4d8664a2d7fd8d0c94`，
  `pragma integrity_check` ok。

## 部署过程

- 部署包 `cloud-release-3.4.2.tgz`（`Teacher-3.4.2.jar` 1,667,574 bytes + 32 个
  运行时依赖，h2 为 test scope 不在内；139,419,728 bytes）上传，本地与远端
  SHA-256 均为
  `cfe0c273888a004526e09d7a5d975be516487b57c8c867d505be21bf4bb1595b`。
- swap 前先 `cp -a` 复制 `runtime/jdk25`（生产 `bin/run-cloud.sh` 经
  `$APP_HOME/runtime/jdk25/bin/java` 启动，`bin/` 由 swap 从 current 复制）。
- `deploy-v111.py swap` 原子切换 `current → releases/3.4.2` 并重启
  `sqlteacher-cloud.service`，结果 `SWAP_OK`（`bin/` 运维脚本自动复制，jar+lib
  移入 `app/`）。
- 更新清单：CI 产出的签名信封上传前在本地完成 Ed25519 验签（公钥
  `release-2026-01`，签名覆盖 762 字节 payload 原始字节，验签 OK），payload
  version=3.4.2、channel=stable、rollout 100% 未暂停，installer/portable SHA-256
  与 CI `SHA256SUMS.txt` 一致（installer `11f19ea1…`、portable `2c4f5b54…`）；
  备份旧清单后 sftp 至 `/tmp` 校验一致，再 `install -o root -g sqlteacher -m 640`
  落地，落地后 SHA-256 == 本地
  `fc3d317906cd7d0e88f3c9e7980075f542182d15d3df25b5484119694107ac54`，1173 bytes
  mode 640 owner root:sqlteacher。服务端按请求读取清单文件，无需重启。

## 上线后验证

- 服务 active/running（启动于 2026-09-15 05:36:25 CST），`/opt/sqlteacher/current
  → /opt/sqlteacher/releases/3.4.2`。
- 回环与公网 HTTPS `/health` 均 `status=ok`（TLS 校验通过、http 200、0.18s），
  knowledgeIndex ready、backlog 0；capabilities 完整（含 BATCH_SUBMISSION_STATUS、
  SIGNED_UPDATES、PROBLEM_REPORTS、REPORT_WITHDRAWAL、REPORT_EXPORT、SESSIONS、
  ACCOUNT_EXPORT、ACCOUNT_DELETION 等）；切换后 error/exception 级日志 0 条。
- **Migration 9 生效**：`cloud_schema_version` 9 个版本全部盖戳（version 9
  "v3.4.1 classroom join codes" 于 2026-09-14 21:36:27 UTC 应用）；`classrooms.join_code`
  列存在；唯一索引 `idx_classrooms_join_code` 创建；存量 4 个班级全部回填互异
  非空短码（`count(*)=4 / count(join_code)=4 / count(distinct)=4`）；服务端日志
  "Backfilled join codes for 4 existing classroom(s)"；`pragma integrity_check` ok。
- **班级码加入端点**：`POST /api/v1/classes/join` 无凭据返回结构化 `403 FORBIDDEN`
  （路由存活且受鉴权保护），对照未知路由返回裸 `404`——非破坏性证明端点已上线。
- **LEG-10 问题反馈冒烟**：匿名 `POST /api/v1/support/reports` → 201
  （reportId `FB-0A0FEAAA97DB`、status RECEIVED）；`POST …/withdraw` → 200
  （WITHDRAWN）；`GET …/{id}` → 200（WITHDRAWN）。匿名记录 `user_id` 为 NULL，
  不出现在任何用户报告历史；已撤回，非待处理报告；`queryToken` 全程未打印/未留存。
- 公网 `/api/v1/app/update-manifest`：http 200，与 CI 资产字节级一致（SHA-256
  `fc3d3179…`），Ed25519 验签 OK，payload version=3.4.2、channel=stable、
  rollout 100% 未暂停。
- 周边服务：qdrant、embedding active；backup、operations-check、qdrant-backup、
  certbot timer 均 active。

## 回滚方案

- 服务回滚：`ln -sfn /opt/sqlteacher/releases/3.4.0 /opt/sqlteacher/current.new
  && mv -T /opt/sqlteacher/current.new /opt/sqlteacher/current
  && systemctl restart sqlteacher-cloud`。Migration 9 为 additive：3.4.0 仅识别
  版本 1–8，会忽略 version-9 盖戳行与未被引用的 `join_code` 列，回滚后仍可正常运行。
- 数据回滚：恢复升级前备份
  `/opt/sqlteacher/backups/cloud-20260914T213535Z.db`（先做当前库副本）。
- 清单回滚：`install -o root -g sqlteacher -m 640
  /opt/sqlteacher/shared/update-manifest.json.backup-20260914T214446Z
  /opt/sqlteacher/shared/update-manifest.json`。

## 已知限制

- 更新清单 `minimumSupportedVersion` 仍为 `1.9.0`（提示闸门），与 v3.4.0 一致；
  是否上抬留待 v3.5.0 评估（数据保护由迁移器强制，与该字段无关）。
- LEG-15 凭据跳过本次首次行使：CI 仍运行 `npm ci`、`npm audit`、打包、签名与
  契约校验，仅跳过重复的 `mvn test`/`npm test` 执行；本地全量测试是该版本测试
  执行的唯一门禁。凭据脚本（`b194f11`）与 workflow（`a3988a9`）的修复均在本次
  发布内完成并随 tag 生效。
- 冒烟遗留一条已撤回的匿名问题反馈记录（`FB-0A0FEAAA97DB`，status WITHDRAWN）
  于 `problem_reports` 表；服务端无清除作业，但该记录对任何用户历史不可见
  （`user_id` NULL）且为终态，不进入待处理队列。
- 桌面本地 `app.db` 的外键运行期强制本版未开启（见发布说明积压）；云端不受影响。

返回 [操作记录索引](README.md)。
