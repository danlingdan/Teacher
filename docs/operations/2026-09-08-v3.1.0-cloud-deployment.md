# SQLTeacher v3.1.0 云端部署与更新清单发布记录

> 部署时间:2026-09-08 09:56–10:07(Asia/Shanghai)
> 生产域名:`https://api.sqlteacher.tech`
> 应用发布:`v3.1.0` 标签(GitHub Actions run `34177517830` 成功并发布)
> 目标目录:`/opt/sqlteacher/releases/3.1.0`

## 部署内容

1. 服务端应用更新到 v3.1.0:新增网络题库分发接口(`GET /api/v1/bank/manifest`、
   `GET /api/v1/bank/block/{type}/{id}`,只读)与管理员发布接口(`POST /api/v1/bank/publish`,
   管理员鉴权;服务端 store `V31ExerciseBankStore` 启动自建表,无既有 schema 变更)。
2. 运行时沿用版本目录内 JDK 25(Temurin 25.0.4.1+1,从 3.0.0 版本目录复制,系统 Java 不受影响)。
3. 稳定通道更新清单替换为 v3.1.0(rollout 100%、未暂停),3.0.0 桌面端的应用内更新从此指向 3.1.0。

## 上线前门禁与状态

- v3.1.0 已通过完整发布门禁并公开 Release(非草稿、非预发布、标记 latest):
  - Java:mvn test 495 项全部通过(2 项预存跳过);
  - 前端:vitest 32 项通过,生产依赖 npm audit 0 漏洞,生产构建无 CSS 警告;
  - Rust:cargo test 4 项通过(桥白名单契约测试在门禁中拦下 7 个缺失方法并已修复);
  - 打包:package-v3.ps1(JDK 25)产出 EXE/ZIP/SHA256SUMS/双 CycloneDX SBOM,
    校验和匹配,ZIP 229 项无敏感内容,无控制台冒烟通过;
  - 打包桌面 E2E:视觉巡检 2 项 + 既有回归 4 项全部通过。
- 本地构建的安装包与 CI 产物为独立构建,发布以 CI 产物为准。
- probe:服务 active,`current → releases/3.0.0`,回环 `/health` ok(backlog 0),
  磁盘剩余约 11 GB。
- 在线备份:`/opt/sqlteacher/backups/cloud-20260908T020039Z.db`,565,248 bytes,
  SHA-256 `f40c84f24e4d69b789e14fa36ccd96a1ace72a129dfea441f4432e9526236605`,
  `pragma integrity_check` ok。

## 部署过程

- 部署包 `cloud-release-3.1.0.tgz`(`Teacher-3.1.0.jar` + 33 个运行时依赖,141,817,496 bytes)
  上传,本地与远端 SHA-256 均为
  `91799a3644a92e29e46066c5007dc2225de89ed4f43132fd2494abd9402ed98d`。
- JDK 25 运行时从 `releases/3.0.0/runtime` 复制到 `releases/3.1.0/runtime`,
  `java -version` 验证为 OpenJDK 25.0.4.1 LTS(2026-08-18)。
- `deploy-v111.py swap` 原子切换 `current → releases/3.1.0` 并重启
  `sqlteacher-cloud.service`,结果 `SWAP_OK`(bin/ 运维脚本自动从当前版本复制)。

## 上线后验证

- 服务 active,`/opt/sqlteacher/current → /opt/sqlteacher/releases/3.1.0`。
- 回环与公网 HTTPS `/health` 均 `status=ok`(TLS 校验通过),知识索引 ready、backlog 0,
  signedUpdates available。
- 服务进程运行于 `/opt/sqlteacher/current/runtime/jdk25/bin/java`。
- **新题库接口**:回环与公网 `GET /api/v1/bank/manifest` 均 200,返回
  `{"bankVersion":0,"datasets":[],"exercises":[]}`(空清单,待教师首次发布)。
- schema 检查:`account_tasks`、`reset_tokens`、`email_verifications` 均在;重启后 3 分钟内
  error/exception 日志为 0。
- 更新清单:CI 产出的签名清单经仓库公钥(`release-2026-01`)Ed25519 验签有效,
  payload 内 installer/portable SHA-256 与 GitHub Release 资产摘要逐一比对一致;
  替换前旧清单备份为
  `/opt/sqlteacher/shared/update-manifest.json.backup-20260908T020517Z`(v3.0.0 stable,
  SHA-256 `36f235083be0eb9795ccfafaa33d4f7137523757ad1bd021efc21925cf4ce040`,与 3.0.0
  部署记录一致);新清单在服务器与本地均为
  `d4530713024f9760f2d36647439499e038510fbbe86f31ffc64f48b69241cf4a`,公网
  `/api/v1/app/update-manifest` 返回内容与之一致,payload version=3.1.0。

## 回滚点

- 应用回滚点:`/opt/sqlteacher/releases/3.0.0`(原子切回 `current` 并重启即可,
  其 bin 脚本使用版本目录内 runtime/jdk25)。
- 数据回滚点:`/opt/sqlteacher/backups/cloud-20260908T020039Z.db`。
- 清单回滚点:`/opt/sqlteacher/shared/update-manifest.json.backup-20260908T020517Z`
  (3.0.0 stable)。

## 已知限制与跟进项

- 题库分发服务当前为空清单(bankVersion 0):需教师在桌面端通过"发布到服务器"
  首次发布内置题库包后,客户端"题库更新"才有内容可拉取。
- 更新清单 rollout 100%:全部 3.0.0 客户端的应用内更新将指向 3.1.0。若升级出现问题,
  优先按"清单回滚点"恢复 3.0.0 清单止血。
- 本次 Cloud schema 无变化(题库 store 表在服务启动时自建);仅在确认数据损坏时才使用
  已验证备份执行恢复流程。
- 备份与运维检查定时器未改动,沿用既有 systemd 单元。

返回 [运维记录索引](README.md)。
