# SQLTeacher v2.3.0 云端部署记录

> 部署时间：2026-08-11 17:45–17:56（Asia/Shanghai）
> 生产域名：`https://api.sqlteacher.tech`
> 发布标签：`v2.3.0`
> 发布提交：`9c76bd389936a5d5576ad83bfb5c4222bc11016b`
> 目标目录：`/opt/sqlteacher/releases/2.3.0`

## 上线前门禁

- GitHub Actions 运行 `31478164716` 成功；正式 Release 为 Latest、非 draft、非 prerelease，
  EXE、便携 ZIP、校验和、CycloneDX SBOM 和签名更新清单五项资产完整。
- 本地复用本轮已经通过的 504 项完整测试结果，并从发布提交执行干净构建；另重跑首页、知识中心、
  分步流程、FXML/视觉资源、环境检测、代码高亮补全、学生发布权限和云服务接口等直接相关测试。
- Windows EXE、便携 ZIP、SBOM 和 app-image 生成成功；包内 `Build-Commit` 与发布提交一致，
  生产 API 地址、校验和、敏感文件扫描和 app-image 启动烟雾测试通过。
- 生产原版本为 `/opt/sqlteacher/releases/2.2.0`，服务 active/enabled，公网与回环 `/health`
  均正常，磁盘剩余约 12 GB。
- 上线前在线备份为 `/opt/sqlteacher/backups/cloud-20260811T094507Z.db`，大小 536,576 bytes，
  SHA-256 为 `3ecee136fdbecd577304fafd0110d4bf11092ced21a6068730f84b6a10891f97`，
  `pragma integrity_check` 为 `ok`。

## 部署过程

- 云端部署包包含发布 JAR 和 51 个运行时依赖，本地与远端 SHA-256 均为
  `08a57d0e12b64cabf0221071c047a2a725e2955715baca355efbd55faadb89dd`。
- 上传并解压到独立版本目录 `/opt/sqlteacher/releases/2.3.0`，复用上一版本的四个运维脚本；
  没有覆盖 2.2.0 回滚目录。
- 以临时链接和 `mv -T` 原子切换 `/opt/sqlteacher/current`，只重启
  `sqlteacher-cloud.service`，服务在健康阈值内一次恢复。
- GitHub Release 的更新清单用仓库内置 Ed25519 公钥验证成功；稳定通道为 2.3.0、
  rollout 100%、未暂停。清单以 `root:sqlteacher`、`0640` 原子替换到共享路径，旧清单保留为
  `/opt/sqlteacher/shared/update-manifest.json.backup-20260811T095407Z`。

## 上线后验证

- `/opt/sqlteacher/current` 指向 `/opt/sqlteacher/releases/2.3.0`；远端 JAR Manifest 的
  `Implementation-Version` 为 `2.3.0`，`Build-Commit` 为发布提交。
- 远端 JAR 与本地发布 JAR 的 SHA-256 均为
  `749efce444c1d1b73e131b6a75b70171bb2144836268f69f938ecd28c0986a61`。
- 回环与公网 HTTPS `/health` 均为 `status=ok`，知识索引 ready、backlog 0；能力接口包含
  Course Package V2、Artifact Sync V2、显式同步冲突和项目元数据同步能力。
- 数据库完整性为 `ok`，18080 仅绑定 `[::ffff:127.0.0.1]:18080`，Nginx 配置通过，
  上线后 warning 及以上日志为 0。
- 公网更新端点与 GitHub Release 清单 SHA-256 均为
  `7daf14a77c64615c1dafba19e106c3dafb4fb6ff78df3ecd8ce5b583e8c2a99c`；版本 2.3.0、
  stable、rollout 100%、未暂停。
- 数据库备份与运维检查定时器 active/enabled；通过带生产环境文件的 systemd 单元执行运维探针，
  `Result=success`、`ExecMainStatus=0`。

## 回滚点

- 应用回滚点：`/opt/sqlteacher/releases/2.2.0`。
- 数据回滚点：`/opt/sqlteacher/backups/cloud-20260811T094507Z.db`。
- 更新清单回滚点：`/opt/sqlteacher/shared/update-manifest.json.backup-20260811T095407Z`。
- 本次 Cloud schema 未变化；应用或清单异常时先原子切回旧 release/清单，只有确认数据损坏时才使用
  已验证备份执行恢复流程。

返回 [运维记录索引](README.md)。
