# 2026-09-20 v3.7.0 发布与更新清单部署记录

- **发布方式**：CI 全流程发布。tag `v3.7.0` 推送触发 `release.yml`（run 35493334056），
  LEG-15 回执被接受（CI 跳过 mvn/npm 测试，构建、npm audit、打包、契约检查照常执行），
  CI 签名 `update-manifest.json` 随 Release 资产发布后部署到 ECS。
- **版本**：`v3.7.0`（教师端学情反馈版本）。提交 `d81743d`（发布内容）+ `b79b9ba`（LEG-15
  回执，`[skip ci]`）+ `2434ca7`（发布触发提交，回执重绑定 tag 父提交）；tag `v3.7.0`
  最终绑定 `2434ca7`。
- **本地门禁**：`mvn test` 826/0/2（全量两次：首次 1 处失败为 `V111AccountStoreTest` 既有
  偶发缺陷——旧断言只校验密码哈希首字节 ≠1，随机哈希首字节恰为 1 时误报；已改为对比完整
  哈希并复跑全量通过）、`npm test` 179、`npm audit --omit=dev` 0 漏洞（dev 链 extract-zip
  新增 13 条 high 通告，仅影响本地 E2E 工具链，不随应用分发，CI audit 亦通过）、
  `npm run build`、`cargo test --lib` 5、`ReleaseVerificationApp` 5 项 PASS、
  `package-v3.ps1` 打包成功并冒烟启动便携版（SQLTeacher 3.7.0 窗口正常）。
- **CI 波折**：首次 tag 推送未触发 release.yml——回执提交信息带 `[skip ci]`，GitHub 对
  tag 推送同样跳过（v3.6.0 记录中有相同现象，当时按用户指示改走本地发布）。修复：在回执
  提交之上追加无 skip 指令的触发提交 `2434ca7` 并重打 tag，回执校验按"绑定 tag 父提交、
  差异仅回执文件"路径通过。中途一次 tag 误指 `d81743d` 导致被检树内无回执文件、CI 回退
  全量测试并暴露一处 CI 环境偶发的 SQLite 初始化错误（`DefaultLocalAppApiInvokeContractTest`
  780 中 1 Error，本地不可复现），回执路径修正后该路径不再执行。
- **Release**：CI 自动发布，非 draft、非 prerelease，7 项资产（EXE、ZIP、SHA256SUMS.txt、
  两个 SBOM、CI 签名 update-manifest.json、verification-receipt.json）。
- **签名清单**：CI 以 `release-2026-01` 密钥签名（payload version 3.7.0、channel stable）。
- **ECS 部署**：部署前备份 `/opt/sqlteacher/shared/update-manifest-v3.6.0.json`；SFTP 上传至
  `/tmp/.update-manifest-v370.tmp`，远端 sha256（39815439…c568af）与本地 Release 资产一致后
  `install -o root -g sqlteacher -m 0640` 原子替换（paramiko 密码认证，凭据不落日志）；
  无需重启服务（清单按请求读取）。
- **公网验证**：`GET /api/v1/app/update-manifest` → keyId `release-2026-01`、version `3.7.0`、
  channel `stable`、installer URL 指向 v3.7.0 Release 资产。
- **回写（DOC-8/9）**：`docs/README.md` 当前基线 → 3.7.0；`docs/releases/README.md` 头部
  与索引；本发布说明日期行（撰写时即按已公开口径）；`docs/plans/README.md` 与计划文档状态；
  GitHub Release 正文替换为发布说明全文。
- **回滚**：如需回退清单，`/opt/sqlteacher/shared/update-manifest-v3.6.0.json` 备份在位，
  原子替换回原名即可（客户端更新检查将回到 3.6.0；已升级到 3.7.0 的客户端不受影响，
  其本地 schema 迁移 27/28 不回退）。
