# SQLTeacher v2.2.0 云端部署记录

> 部署时间：2026-08-09 23:04（Asia/Shanghai）
> 生产域名：`https://api.sqlteacher.tech`
> 发布标签：`v2.2.0`
> 发布提交：`2b3ddf984b23df0b25123a421fbb6ecdc99361d9`
> 目标目录：`/opt/sqlteacher/releases/2.2.0`

## 上线前门禁

- GitHub Actions 运行 `31319773104` 成功；正式 Release 为 Latest、非 draft、非 prerelease，
  EXE、便携 ZIP、校验和、SBOM 和签名更新清单五项资产完整。
- 本地复用发布前刚完成的完整测试结果：487 项测试，0 失败、0 错误、2 项按设计跳过；
  Markdown 本地链接检查覆盖 218 个文件且无断链。
- Windows EXE、便携 ZIP、SBOM 和 app-image 生成成功；包内 `Build-Commit` 与发布提交一致，
  生产 API 地址、校验和和敏感文件扫描通过，app-image 启动冒烟通过。
- 生产原版本为 `/opt/sqlteacher/releases/2.0.0`，服务 active/enabled，Cloud schema 6，
  数据库 `pragma integrity_check` 为 `ok`，18080 仅绑定回环地址。
- 上线前在线备份为 `/opt/sqlteacher/backups/cloud-20260809T150154Z.db`，大小 536,576 bytes，
  SHA-256 为 `bad47614e613b2ff37f86803ec55a6f5014e5894c3cb3e1f60e0737835daad38`，完整性为 `ok`。

## 部署过程

- 云端部署 ZIP 包含发布 JAR、51 个运行时依赖和四个运维脚本；本地与远端 SHA-256 均为
  `5c86a833abaf68f28792737b22917c032656138be02dfc0f4ceeb66a2ec2ceef`。
- 在独立 staging 目录使用 Python 标准库解压并核对发布 JAR，随后移动为版本目录；没有覆盖旧版本目录。
- 以新 symlink 和 `mv -Tf` 原子替换 `/opt/sqlteacher/current`，只重启
  `sqlteacher-cloud.service`。启动最初两次健康探测短暂连接失败，随后在回滚阈值内通过，未触发回滚。
- GitHub Release 清单使用仓库内置 Ed25519 公钥验证成功，稳定通道为 2.2.0、rollout 100%、
  未暂停；清单以 `root:sqlteacher`、`0640` 原子替换到共享路径。
- 旧清单保留为
  `/opt/sqlteacher/shared/update-manifest.json.backup-20260809T150154Z`。

## 上线后验证

- `/opt/sqlteacher/current` 指向 `/opt/sqlteacher/releases/2.2.0`，远端 JAR Manifest 的
  `Build-Commit` 为发布提交，`sqlteacher-cloud.service` active/enabled。
- 回环与公网 HTTPS `/health` 均为 `status=ok`；健康页 `apiVersion=1.11` 是云端接口协议版本，
  不是应用发布版本。
- Cloud schema 保持 6，18080 仅监听 `[::ffff:127.0.0.1]:18080`；上线后服务日志中
  warning 及以上记录为 0。
- 公网更新端点返回 `stable/2.2.0`、rollout 100%、未暂停；线上清单 SHA-256 为
  `907e67c520861d15e1239ce321589b693fb0a1d6c5296c46e01f3e796772075d`，与 GitHub Release 资产一致。
- 数据库备份、运维检查和 Certbot 定时器均为 active。

## 回滚点

- 应用回滚点：`/opt/sqlteacher/releases/2.0.0`。
- 数据回滚点：`/opt/sqlteacher/backups/cloud-20260809T150154Z.db`。
- 更新清单回滚点：
  `/opt/sqlteacher/shared/update-manifest.json.backup-20260809T150154Z`。
- 本次 Cloud schema 未变化；应用或清单异常时可先原子切回旧 release/清单，只有确认数据损坏时
  才使用已验证备份执行恢复流程。
