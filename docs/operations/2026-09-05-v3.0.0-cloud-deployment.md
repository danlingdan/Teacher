# SQLTeacher v3.0.0 云端部署与更新清单发布记录

> 部署时间:2026-09-05 15:10–15:25(Asia/Shanghai)
> 生产域名:`https://api.sqlteacher.tech`
> 应用发布:服务端随 v3.0.0 主线构建(部署提交 `ed11bf2`,与 `v3.0.0` 标签的服务端代码一致,仅相差后续文档提交)
> 目标目录:`/opt/sqlteacher/releases/3.0.0`

## 部署内容

1. 服务端应用更新到 3.0.0 主线(获得 Qdrant/Ollama 嵌入客户端的 HTTP/1.1 锁定修复;无 API、schema 变更)。
2. 服务端运行时新增独立 JDK 25(Temurin 25.0.4.1+1 LTS,置于版本目录内,系统 Java 21 不受影响)。
3. 稳定通道更新清单替换为 v3.0.0(rollout 100%、未暂停),2.3.0 桌面端的应用内更新从此指向 3.0.0。

## 上线前门禁与状态

- v3.0.0 已通过完整发布门禁并公开 Release(GitHub Actions run `33949676099` 成功)。
- 3.0.0 客户端与 2.3.0 服务端的 API 兼容性经代码核对:3.0 开发期间服务端仅 2 个 HTTP 客户端文件变更,
  无新端点、无 schema 变更;客户端调用的全部 `v14`/`v19`/`v20` 端点在 2.3.0 服务器已存在。
- probe:服务 active/enabled,`current → releases/2.3.0`,回环 `/health` ok(知识索引 ready、backlog 0),
  磁盘剩余约 12 GB。
- 在线备份:`/opt/sqlteacher/backups/cloud-20260905T071052Z.db`,552,960 bytes,
  SHA-256 `ca884187abb4aa76ae62371513e1e730842a12ccffcbfa8514af95ef7041083e`,`pragma integrity_check` ok。

## 部署过程

- 部署包 `cloud-release-3.0.0.tgz`(`Teacher-3.0.0.jar` + 33 个运行时依赖)上传,本地与远端
  SHA-256 均为 `4a5935046c83a3e8f1a12335cb9ee935a07a82452178b33da822c809e74ca243`。
- JDK 25 经清华 TUNA 镜像在 ECS 本地下载(`OpenJDK25U-jre_x64_linux_hotspot_25.0.4.1_1.tar.gz`,
  61,718,288 bytes),解压至 `/opt/sqlteacher/releases/3.0.0/runtime/jdk25`,`java -version` 验证为
  Temurin 25.0.4.1+1 LTS。
- `bin/` 复制自 2.3.0 并保留全部运维脚本;`run-cloud.sh` 改为使用版本目录内
  `runtime/jdk25/bin/java`(回滚到 2.3.0 时旧脚本仍使用系统 Java 21,互不影响)。
- `deploy-v111.py swap` 原子切换 `current → releases/3.0.0` 并重启 `sqlteacher-cloud.service`,
  结果 `SWAP_OK`(服务一次恢复;启动失败会自动保留旧版本回滚点)。

## 上线后验证

- 服务 active,`/opt/sqlteacher/current → /opt/sqlteacher/releases/3.0.0`。
- 回环与公网 HTTPS `/health` 均 `status=ok`,知识索引 ready、backlog 0,signedUpdates available。
- 回环 `/api/v1/app/capabilities`:apiVersion 2.0,SIGNED_UPDATES、COURSE_PACKAGE 等能力完整。
- schema 检查:`account_tasks`、`reset_tokens`、`email_verifications` 均在;服务重启后 2 分钟内
  error/exception 日志为空。
- 更新清单:新清单在本地以仓库公钥(`release-2026-01`)完成 Ed25519 验签(payload:v3.0.0、stable、
  rollout 100%、未暂停),替换前旧清单备份为
  `/opt/sqlteacher/shared/update-manifest.json.backup-20260905T072200Z`;公网
  `/api/v1/app/update-manifest` 返回内容与服务器共享文件 SHA-256 一致
  (`36f235083be0eb9795ccfafaa33d4f7137523757ad1bd021efc21925cf4ce040`)。

## 回滚点

- 应用回滚点:`/opt/sqlteacher/releases/2.3.0`(原子切回 `current` 并重启即可,旧脚本使用系统 Java 21)。
- 数据回滚点:`/opt/sqlteacher/backups/cloud-20260905T071052Z.db`。
- 清单回滚点:`/opt/sqlteacher/shared/update-manifest.json.backup-20260905T072200Z`(2.3.0 stable)。

## 已知限制与跟进项

- 更新清单 rollout 100%:全部 2.3.0 客户端的应用内更新将指向 3.0.0。维护者已知情并选择直接发布;
  2.3.0→3.0.0 的真机升级矩阵仍未执行,若出现升级问题,优先按"清单回滚点"恢复 2.3.0 清单止血。
- 本次 Cloud schema 未变化;仅在确认数据损坏时才使用已验证备份执行恢复流程。
- 备份与运维检查定时器未改动,沿用既有 systemd 单元。

返回 [运维记录索引](README.md)。
