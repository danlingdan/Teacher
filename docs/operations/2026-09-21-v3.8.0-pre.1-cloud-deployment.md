# SQLTeacher v3.8.0-pre.1/pre.2 云端服务端部署记录

> 部署时间：2026-09-21 05:05–05:08（Asia/Shanghai，UTC+8）
> 生产域名：`https://api.sqlteacher.tech`
> 部署版本目录：`/opt/sqlteacher/releases/3.8.0-pre.1`（未发布预览版，代码为 v3.8.0 计划
> ACC 批次实施内容，基于 `main` 未提交工作区构建；jar 由 `Teacher-3.7.0.jar` 复制重命名）
> 执行方式：`packaging/cloud/deploy-v111.py` 分阶段 + `ssh-run.py`（paramiko，凭据不落日志）

## 部署前状态（probe）

- `current → /opt/sqlteacher/releases/3.4.3`，服务 active；**生产服务端此前为 3.4.3**
  （v3.5.x–v3.7.0 各版本仅发布清单/客户端，服务端未升级——本次部署一并补齐跨越的服务端能力）。
- 系统 java 为 OpenJDK 21，实际运行使用版本目录内 `runtime/jdk25`（25.0.4.1+1 LTS，
  从 3.4.3 目录复制并 `java -version` 验证）。
- 回环 `/health` ok（backlog 0），磁盘剩 8.4 GB。

## 升级内容（3.4.3 → 3.8.0-pre.1 服务端跨度）

- **云端 schema 迁移 10 + 11**：`assignment_submissions.submission_payload_json`（v3.7.0 S3）
  与 `role_grant_codes` 一次性教师升级码表（v3.8.0 ACC-S4，决策点 1 方案 B）。
- **v3.7.0 TFB-S1–S4 服务端能力首次上线**：教师读学生事件明细、班级总览、提交载荷、
  上传限流（1 次/分）、每用户 10 万条软上限、`sync_events` 180 天自动清理（启动 10 秒后首跑）。
- **v3.8.0 ACC-S1–S4**：SMTP 邮件通道（未配置时降级文件 outbox）、6 位验证码化密码重置/
  邮箱绑定验证（重置不再要求 `email_verified`）、`PATCH /api/v1/account/profile`、
  登录/注册/刷新可选 `deviceLabel`、教师升级码签发/列表/撤销/兑换端点、统一错误文案防枚举。
- 旧版 3.7.0 客户端 additive 兼容（新端点/新入参不破坏既有路由与响应）。

## 备份与回滚点

- 升级前在线备份：`/opt/sqlteacher/backups/cloud-20260920T210545Z.db`
  （1,449,984 bytes，SHA-256 `ec8bf64d95ef4adf3e7ae17bb7d27cd595ad906cb3861b30569df303153838ad`，
  `pragma integrity_check` ok）。
- 应用回滚点：`/opt/sqlteacher/releases/3.4.3`（原子切回 `current` 并重启即可）。

## 部署过程

- 部署包 `cloud-release-3.8.0-pre.1.tgz`（`Teacher-3.8.0-pre.1.jar` + 36 个运行时依赖，
  134 MB）本地/远端 SHA-256 一致：`bc2c8f902ccaa22f466ea94682949690e8ef9d25075e1dedbee9e880efd2e3ea`。
- 新增依赖：`org.eclipse.angus:angus-mail:2.0.5`（含 `jakarta.mail-api`、`angus-activation`）。
- 版本目录组装后复制 3.4.3 的 `bin/` 与 `runtime/`（服务器 run-cloud.sh 使用
  `$APP_HOME/runtime/jdk25/bin/java`，与仓库内 `/usr/bin/java` 版本已漂移，以服务器版本为准）。
- `deploy-v111.py swap`：`current → releases/3.8.0-pre.1` 原子切换，`sqlteacher-cloud.service`
  重启后 SWAP_OK。

## 上线后验证

- 服务 active；回环与公网 HTTPS `/health` 均 `status=ok`（apiVersion 1.11、知识索引 ready、
  backlog 0）。
- 云端 schema：`cloud_schema_version` max = **11**；`role_grant_codes` 表已创建。
- 新端点路由确认：`POST /api/v1/admin/role-codes` 匿名请求回环 403、公网 403（鉴权拒绝而非
  404）；`POST /api/v1/account/role-codes/redeem` 未登录 401。
- 邮件通道：`SQLTEACHER_CLOUD_SMTP_*` 未配置（cloud.env 未变更），按设计降级文件 outbox
  `/opt/sqlteacher/data/mails`（当前 0 封）。

## 追加部署：v3.8.0-pre.2（2026-09-21 05:26）

- **原因**：`SmtpMailSender` 原先仅支持 STARTTLS（587）；用户选定阿里云邮件推送（smtpdm.aliyun.com，
  端口 25/80/465，ECS 出方向 25 默认封禁，465 为隐式 SSL）——为服务端新增
  `SQLTEACHER_CLOUD_SMTP_SSL=true` 隐式 SSL 支持（`mail.smtp.ssl.enable` + SSLSocketFactory，
  与 STARTTLS 互斥），配置级测试覆盖（SSL 握手路径经真实 smtpdm 连通验证，假凭据返回
  AuthenticationFailedException 符合预期）。
- 部署包 `cloud-release-3.8.0-pre.2.tgz`（36 个运行时依赖）本地/远端 SHA-256 一致：
  `1327ba5370f57e143cbb414cc799605a4a6cc5560c0ebb7c15744939fb44a5c3`。
- 升级前备份：`/opt/sqlteacher/backups/cloud-20260920T212602Z.db`
  （SHA-256 `f9d76f10e577f8010d592886e44215d0a025a53e25cb58f1872fd02c1c645e07`，integrity ok）。
- `swap` SWAP_OK；回环与公网 `/health` ok，journal 2 分钟内无 error。应用回滚点新增
  `releases/3.8.0-pre.1`。
- cloud.env 的 SMTP 六项待用户提供阿里云发信地址与 SMTP 密码后写入并重启生效。

## SMTP 配置完成(2026-09-21 05:44)

- 阿里云邮件推送域名 `mail.sqlteacher.tech` 的 SPF/DMARC/MX/DKIM 四条记录经公网与
  阿里公共 DNS(223.5.5.5)双端验证生效;发信地址 `noreply@mail.sqlteacher.tech`(触发邮件)。
- `/etc/sqlteacher/cloud.env` 写入 `SQLTEACHER_CLOUD_SMTP_*` 六项(HOST=smtpdm.aliyun.com、
  PORT=465、SSL=true,凭据不记录于本文档),追加前后行数校验(0→6),权限保持
  `root:sqlteacher 0640`;服务重启后回环 `/health` ok。
- **真实发信验证通过**：服务器侧经 465/SSL 登录并发信至用户 QQ 邮箱成功（SMTP_TEST_OK）；
  密码重置/邮箱验证码自此为真实邮件投递。本地凭据存于 gitignored `.secrets/smtp-directmail.env`。

## 已知限制与跟进项

- **SMTP 未配置**：密码重置/邮箱验证码写服务器文件 outbox，公网用户实际收不到邮件；
  手测验证码需登录 ECS 读取 outbox 文件，或尽快配置 `/etc/sqlteacher/cloud.env` 的
  `SQLTEACHER_CLOUD_SMTP_*` 六项（见 guide/12）并重启服务。
- 本次部署使 v3.7.0 的服务端能力（提交载荷、教师明细/总览、上传限流与 180 天保留）首次生效；
  此前 v3.7.0 客户端的教师明细/总览视图在生产环境会 404/无数据。
- 3.8.0-pre.1 为未发布代码的预览部署；正式发布时以 CI 产物按发布流程重新部署并统一版本号。
- 安装器重打包待办：首打产物因 sidecar 旧版 jar 污染已作废（`mvn clean` 已清除），
  待手测应用关闭后重打。
