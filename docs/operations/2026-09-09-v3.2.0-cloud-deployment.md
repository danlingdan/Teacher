# SQLTeacher v3.2.0 云端部署与更新清单发布记录

> 部署时间:2026-09-09 02:56–03:25(Asia/Shanghai)
> 生产域名:`https://api.sqlteacher.tech`
> 应用发布:`v3.2.0` 标签(GitHub Actions run `34266105386` 成功并发布)
> 目标目录:`/opt/sqlteacher/releases/3.2.0`

## 部署内容

1. 服务端应用更新到 v3.2.0(v3.2.0 审计修复计划的 W3 服务端加固):
   - 认证端点限速与锁定(login 连续失败 5 次锁 15 分钟、注册/重置邮件限频、
     不存在账号哑 PBKDF2 拉平时序);
   - `/sync/events` 与 `/bank/publish` 请求体上限(1 MiB / 2 MiB,超限 413),
     同步事件单条 payloadJson 限 16 KB;
   - 匿名问题报表限流改读受信代理 X-Forwarded-For 末位并加 installId 维度限额;
   - 邮箱验证令牌原子消费并真正更新 `users.email`,验证邮件限频;
   - 密码重置令牌失败计数生效;V19 干预确认令牌常量时间比较。
2. 运行时沿用版本目录内 JDK 25(Temurin 25.0.4.1+1,从 3.1.0 版本目录复制)。
3. 稳定通道更新清单替换为 v3.2.0(rollout 100%、未暂停),3.1.0 桌面端应用内更新从此指向 3.2.0。

## 上线前门禁与状态

- v3.2.0 已通过完整发布门禁并公开 Release(非草稿、非预发布、`releases/latest` 指向 v3.2.0):
  - Java:发布提交 `31950fa` 上 `mvn test` 558 项全部通过(2 项预存跳过);
  - 前端:vitest 16 文件 42 项通过,生产构建成功;
  - Rust:cargo test 4 项通过(含 Rust 白名单与机器可读清单一致性契约测试);
  - 打包:本地 package-v3.ps1(JDK 25)产出 EXE/ZIP/SHA256SUMS/双 CycloneDX SBOM,
    校验和匹配,ZIP 229 项无敏感内容,无控制台冒烟通过;
  - 发布以 CI 产物为准(CI 独立构建并验签清单)。
- 桌面端 GUI 验证(dev 构建 + v3.2.0 sidecar):首 run 安全模式选择框、教学模式
  CREATE 确认、DROP TABLE/GRANT 确认可执行、CTE 直接执行、连接失败错误细节(D6)
  均在真实界面上验证通过。
- probe:服务 active,`current → releases/3.1.0`,回环 `/health` ok(backlog 0),
  磁盘剩余约 11 GB。
- 在线备份:`/opt/sqlteacher/backups/cloud-20260908T185850Z.db`,577,536 bytes,
  SHA-256 `34c1642b9cca9731bfa30cff174d7ece1fbc7965000297f467412dae5e15500c`,
  `pragma integrity_check` ok。

## 部署过程

- 部署包 `cloud-release-3.2.0.tgz`(`Teacher-3.2.0.jar` + 33 个运行时依赖,141,840,365 bytes)
  上传,本地与远端 SHA-256 均为
  `5984f25b42914e1e930b333ead5f01619fe6e8a3faefd3e4977be930b2eaedd1`。
- JDK 25 运行时从 `releases/3.1.0/runtime` 复制到 `releases/3.2.0/runtime`,
  `java -version` 验证为 OpenJDK 25.0.4.1 LTS(2026-08-18)。
- `deploy-v111.py swap` 原子切换 `current → releases/3.2.0` 并重启
  `sqlteacher-cloud.service`,结果 `SWAP_OK`(bin/ 运维脚本自动从当前版本复制)。

## 上线后验证

- 服务 active,`/opt/sqlteacher/current → /opt/sqlteacher/releases/3.2.0`。
- 服务进程运行于 `/opt/sqlteacher/current/runtime/jdk25/bin/java`,classpath 指向
  `Teacher-3.2.0.jar`。
- 回环与公网 HTTPS `/health` 均 `status=ok`(TLS 校验通过),知识索引 ready、backlog 0,
  signedUpdates available。
- **认证限速生产抽检**:对不存在账号连续 6 次错误密码登录,前 5 次返回 401,
  第 6 次返回 429(`RATE_LIMITED`),新限速代码在生产生效。
- schema 检查:`account_tasks`、`reset_tokens`、`email_verifications` 均在;重启后
  5 分钟内 error/exception 日志为 0。
- 更新清单:CI 产出的签名清单经仓库公钥(`release-2026-01`)Ed25519 验签有效,
  payload 内 installer/portable SHA-256 与 CI `SHA256SUMS.txt` 一致;替换前旧清单
  备份为 `/opt/sqlteacher/shared/update-manifest.json.backup-20260908T192223Z`
  (3.1.0 stable,SHA-256 `d4530713024f9760f2d36647439499e038510fbbe86f31ffc64f48b69241cf4a`,
  与 3.1.0 部署记录一致);新清单在服务器与本地均为
  `5c0587e154491d73244e230f17c22059ff162dedb3a3e7d391ccc41421c3153b`,公网
  `/api/v1/app/update-manifest` 返回内容与之一致(字节级),payload version=3.2.0、
  rollout 100%、未暂停,公网内容再验签通过。

## 回滚点

- 应用回滚点:`/opt/sqlteacher/releases/3.1.0`(原子切回 `current` 并重启即可)。
- 数据回滚点:`/opt/sqlteacher/backups/cloud-20260908T185850Z.db`。
- 清单回滚点:`/opt/sqlteacher/shared/update-manifest.json.backup-20260908T192223Z`
  (3.1.0 stable)。

## 已知限制与跟进项

- 认证限速按账号锁定意味着攻击者可故意锁死某邮箱 15 分钟(规格固有取舍,
  429 响应含 Retry-After);锁定状态为进程内存态,服务重启即清零。
- 本地构建的安装包与 CI 产物为独立构建,发布以 CI 产物为准;清单哈希与 CI
  资产逐一比对一致。
- 本次 Cloud schema 无变化(email_verifications 等表已存在,消费逻辑为行为修复);
  仅在确认数据损坏时才使用已验证备份执行恢复流程。
- 备份与运维检查定时器未改动,沿用既有 systemd 单元。
- W1.4(SQL 确认/取消审计事件)为计划内可选项,本轮未实现;队列 N+1 批量端点
  等 P2 项在 v3.3 候选清单。

返回 [运维记录索引](README.md)。
