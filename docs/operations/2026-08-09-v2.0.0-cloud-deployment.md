# SQLTeacher v2.0.0 云端部署记录

> 部署时间：2026-08-09 17:18（Asia/Shanghai）
> 生产域名：`https://api.sqlteacher.tech`
> 发布标签：`v2.0.0`
> 发布提交：`35a7dc829e0d8133d55d0e9fed30fd237ab03dda`
> 目标目录：`/opt/sqlteacher/releases/2.0.0`

## 上线前门禁

- GitHub Actions 运行 `31304956844` 成功，正式 Release 为 Latest、非 draft、非 prerelease，5 个资产完整。
- 本地从发布提交执行 465 项完整测试，0 失败、0 错误，2 项真实 AI 测试按设计跳过；Windows 包、SBOM、敏感扫描和 app-image 冒烟通过。
- 生产原版本为 `/opt/sqlteacher/releases/2.0.0-alpha.7`，服务 active/enabled，Cloud schema 6，数据库 `pragma integrity_check` 为 `ok`，18080 仅绑定回环。
- 上线前在线备份为 `/opt/sqlteacher/backups/cloud-20260809T091633Z.db`，大小 536,576 bytes，SHA-256 `9157639ba718c3270b0cbb831e24d93de17c5b2fb8baae7a859650ddd7785676`，完整性为 `ok`。

## 部署过程

- 云端部署 ZIP 包含发布 JAR、44 个运行时依赖和四个运维脚本；本地与远端 SHA-256 均为 `380062ad43f61dd37e2b68b0eeb20e0063543ae2ef605f6fd7c6447e1680cdaf`。
- 远端没有安装 `unzip`，首次准备在解压前安全停止；确认新版本目录为空后，使用 Python 标准库解压同一已校验 ZIP，没有安装额外软件。
- JAR SHA-256 为 `f22288196655c9e7d78d884610a007937a7faae984a08658da530f81e148c829`；Manifest 的版本和 `Build-Commit` 与标签提交一致。
- 创建新 symlink 后以 `mv -Tf` 原子替换 `/opt/sqlteacher/current`，只重启 `sqlteacher-cloud.service`；回环健康首次通过，未触发自动回滚。
- 将 GitHub Release 的签名清单以 `root:sqlteacher`、`0640` 原子替换到共享路径，旧清单保留为 `/opt/sqlteacher/shared/update-manifest.json.pre-2.0.0`。

## 上线后验证

- `/opt/sqlteacher/current` 指向 `/opt/sqlteacher/releases/2.0.0`，`sqlteacher-cloud.service` active。
- 回环与公网 HTTPS `/health` 均为 `status=ok`，知识索引 ready、backlog 0、签名更新可用。
- 公网能力端点报告 `apiVersion=2.0`、最低客户端 1.9.0、16 个能力、maintenance=false。
- Cloud schema 保持 6，生产数据库和上线前备份完整性均为 `ok`；18080 只监听 `[::ffff:127.0.0.1]:18080`。
- 生产更新端点提供 `stable/2.0.0`、rollout 100%、未暂停；清单 SHA-256 `2f97937144e55fa2587ce3c3e5e838162f6a90dac2d3f5d384ee6c11942b81ae` 与 GitHub Release 资产一致。
- `sqlteacher-operations-check.service` 结果 success、退出码 0；数据库备份、运维检查、Certbot、Qdrant、Qdrant 备份和 Embedding 单元均 active。
- 部署后 `sqlteacher-cloud.service` 日志中 `error|exception|failed` 命中 0。

## 回滚点

- 应用回滚点：`/opt/sqlteacher/releases/2.0.0-alpha.7`。
- 数据回滚点：`/opt/sqlteacher/backups/cloud-20260809T091633Z.db`。
- 更新清单回滚点：`/opt/sqlteacher/shared/update-manifest.json.pre-2.0.0`。
- 本次 Cloud schema 未变化；若只发生应用或清单问题，可先原子切回旧 release/清单。只有确认数据受损或不兼容时才使用已验证备份执行恢复流程。
